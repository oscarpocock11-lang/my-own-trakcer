{
  "name": "stock-market-monitor",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
